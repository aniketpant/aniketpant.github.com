# direnv evaluates .envrc with its own (non-interactive) shell, so nvm is not
# available by default — source it explicitly before using it.
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

nvm use