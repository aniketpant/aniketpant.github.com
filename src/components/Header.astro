---
import ThemeToggle from './ThemeToggle.astro';

interface Props {
  isHome?: boolean;
}

const { isHome = false } = Astro.props;

const navItems = [
  { label: 'About', href: '/about/' },
  { label: 'Speaking', href: '/speaking/' },
  { label: 'Archive', href: '/archive/' },
];
---

<header class="sticky top-0 z-50 backdrop-blur-md bg-[var(--bg)]/80 border-b border-[var(--border)]" role="banner">
  <div class="mx-auto max-w-3xl px-6 md:px-8">
    <div class="flex items-center justify-between h-14">
      <!-- Logo -->
      <div class="flex-shrink-0">
        {isHome ? (
          <h1 class="text-xl font-medium tracking-tight">
            <a href="/" rel="nofollow" class="group flex items-center gap-0.5 text-[var(--fg)] hover:text-[var(--color-brand)] transition-colors duration-200">
              <span class="font-bold">no</span><span>remap</span>
            </a>
          </h1>
        ) : (
          <a href="/" rel="nofollow" class="group flex items-center gap-0.5 text-xl font-medium tracking-tight text-[var(--fg)] hover:text-[var(--color-brand)] transition-colors duration-200">
            <span class="font-bold">no</span><span>remap</span>
          </a>
        )}
      </div>

      <!-- Nav + Toggle -->
      <div class="flex items-center gap-1">
        <nav aria-label="Main navigation">
          <ul class="flex items-center gap-1">
            {navItems.map((item) => (
              <li>
                <a
                  href={item.href}
                  class="px-3 py-1.5 rounded-md text-sm font-medium text-[var(--fg-muted)] hover:text-[var(--fg)] hover:bg-[var(--bg-secondary)] transition-all duration-200"
                >
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
        <div class="ml-2 pl-2 border-l border-[var(--border)]">
          <ThemeToggle />
        </div>
      </div>
    </div>
  </div>
</header>
